import { IncompleteMatchSelectorPipe } from './incomplete-match-selector.pipe';

describe('IncompleteMatchSelectorPipe', () => {
  it('create an instance', () => {
    const pipe = new IncompleteMatchSelectorPipe();
    expect(pipe).toBeTruthy();
  });
});
