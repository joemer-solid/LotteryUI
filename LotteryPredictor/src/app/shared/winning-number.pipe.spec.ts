import { WinningNumberPipe } from './winning-number.pipe';

describe('WinningNumberPipe', () => {
  it('create an instance', () => {
    const pipe = new WinningNumberPipe();
    expect(pipe).toBeTruthy();
  });
});
